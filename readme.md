# 🚀 Fullstack Turbo Starter Kit (2026)

Современный монорепозиторий на базе **Turborepo**, объединяющий строгую типизацию по всему стеку, высокопроизводительный API и масштабируемую дизайн-систему.

## 🛠 Стек технологий

- **Monorepo:** [Turborepo](https://turbo.build/) + [pnpm](https://pnpm.io/)
- **Frontend:** [Next.js 15+](https://nextjs.org/) (App Router), [Tailwind CSS](https://tailwindcss.com/)
- **Backend:** [NestJS](https://nestjs.com/) + [tRPC v11](https://trpc.io/)
- **Database:** [Prisma](https://www.prisma.io/) + SQLite/PostgreSQL
- **Auth:** JWT via **HttpOnly Cookies**
- **Validation:** [Zod](https://zod.dev/) (Shared schemas)
- **Documentation:** [Storybook](https://storybook.js.org/)

---

## 📂 Структура проекта

```text
.
├── apps/
│   ├── web/                # Next.js приложение (фронтенд)
│   └── api/                # NestJS сервер (точка входа API)
├── packages/
│   ├── trpc/               # Логика tRPC: роутеры, контекст и процедуры
│   ├── database/           # Prisma Client, схема и миграции
│   ├── shared/             # Общие Zod-схемы и типы (NodeNext ESM)
│   ├── ui/                 # Библиотека UI компонентов + Storybook
│   └── config-eslint/      # Конфигурация линтинга
├── turbo.json              # Конфигурация Turborepo
└── pnpm-workspace.yaml     # Конфигурация воркспейсов
```

⚙️ Быстрый старт

1. Установка зависимостей
   pnpm install

2. Настройка окружения
   Создайте .env в корне проекта:

   DATABASE_URL="postgresql://user:password@localhost:5432/mydb?schema=public"
   JWT_SECRET="change_me"
   NODE_ENV="development"

📝 Команды

pnpm dev Запуск всех приложений в dev-режиме
pnpm build Сборка всех пакетов и приложений
pnpm db:studio GUI для управления базой данных
pnpm type-check Глобальная проверка типов

🔐 Особенности архитектуры
Shared Schemas & Types
Мы используем пакет @core/shared для хранения Zod-схем. Это позволяет валидировать данные на бэкенде и автоматически получать типизированные формы на фронтенде без дублирования кода.

Безопасная авторизация
Авторизация реализована через серверные куки (httpOnly). Это исключает кражу токенов через XSS-атаки. Для проверки состояния сессии на фронтенде используется кастомный хук useAuth.

NodeNext & ESM
Проект настроен на использование современных стандартов модулей Node.js, что обеспечивает высокую скорость сборки и совместимость с актуальными библиотеками 2026 года.
