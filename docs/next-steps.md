# 🚀 Next Steps — запуск первых фич

> Мы только что собрали боевую инфраструктуру. Агенты готовы, MCP подключён, Docker поднят.
> Теперь — стартовая черта. Что делаем дальше?

---

## ✅ Чеклист готовности инфраструктуры

Прежде чем жать "запустить" — убедись, что всё зелёное.

### MCP серверы (`.mcp.json`)

- [ ] `filesystem` — доступ к файлам проекта
- [ ] `postgres` — подключён через `DATABASE_URL` в env (не в CLI!)
- [ ] `git` — история и diff для агентов
- [ ] `playwright` — браузерное тестирование (`@playwright/mcp@latest`)
- [ ] `shadcn-mcp-server` — компоненты shadcn/ui для frontend-ui агента

Проверить, что Claude Code подхватил серверы:

```bash
# Открой Claude Code и запусти:
/mcp
# Должны появиться все 5 серверов со статусом connected
```

### Docker (`docker-compose.yml`)

- [ ] `postgres:17-alpine` на порту 5432 (с healthcheck)
- [ ] `redis:7-alpine` на порту 6379 (с healthcheck)
- [ ] `chromadb/chroma` на порту 8000 (с healthcheck)

```bash
docker compose up -d
docker compose ps   # все сервисы должны быть healthy
```

### Environment (`.env`)

- [ ] `ANTHROPIC_API_KEY` — обязателен
- [ ] `GOOGLE_API_KEY` — для Gemini orchestrator (можно пропустить в режиме Claude Code)
- [ ] `DATABASE_URL` — postgresql://...
- [ ] `REDIS_URL` — redis://localhost:6379
- [ ] `CHROMA_URL` — http://localhost:8000
- [ ] `BASE_URL` — http://localhost:3000 (нужен regression testing engine)
- [ ] `BUDGET_LIMIT` — рекомендуется 20 (USD/месяц)

```bash
cp .env.example .env
# Заполни ключи — всё остальное работает с дефолтами
```

### TypeScript / Build

- [ ] Typecheck проходит без ошибок

```bash
pnpm typecheck              # весь монорепо
pnpm --filter @core/ai-agents typecheck   # только агенты
```

---

## 🔀 Два пути: Claude Code vs TypeScript Orchestrator

Инфраструктура поддерживает оба способа запуска. Они не конкурируют — используй тот, что подходит для задачи.

### Путь 1 — Claude Code (интерактивный)

**Когда использовать:** разработка фич вручную, экспериментирование, быстрый прототип.

```
Ты → Claude Code → /orchestrator → sub-agents (.claude/agents/*.md)
```

**Агенты доступны сразу:**

| Агент               | Что делает                              |
| ------------------- | --------------------------------------- |
| `orchestrator`      | Декомпозирует задачу, управляет потоком |
| `architect`         | Security review, quality gate (Phase 1) |
| `architect-review`  | Архитектурный ревью по запросу          |
| `frontend-ui`       | React 19, Tailwind v4, shadcn/ui, a11y  |
| `frontend-bizlogic` | Zustand, Zod, TanStack Query            |
| `backend-api`       | NestJS 11, JWT/OAuth2, guards, DTOs     |
| `backend-database`  | Prisma 7, PostgreSQL, migrations        |
| `qa-testing`        | Vitest, Playwright E2E                  |
| `context-manager`   | Инжектирует навыки в агентов            |
| `prompt-engineer`   | Улучшает промпты                        |

**Запуск:**

```
"Реализуй фичу: страница профиля пользователя с аватаром, именем и кнопкой выхода"
```

Claude Code сам вызовет orchestrator, тот — нужных суб-агентов.

---

### Путь 2 — TypeScript Orchestrator (автономный)

**Когда использовать:** batch-запуск нескольких фич, 24/7 режим, CI/CD интеграция, бюджетный контроль.

```
FeatureRequest → MVPOrchestrator → Gemini (decompose) → Claude sub-agents → результат
```

**Компоненты:**

- `MVPOrchestrator` — главный цикл, роутинг по агентам
- `FeatureQueue` (BullMQ) — очередь задач с Redis
- `VectorStore` (ChromaDB) — checkpoint контекста между сессиями
- `TelegramNotifier` — уведомления (опционально)

**Запуск одиночной задачи:**

```bash
# Сначала собери
pnpm --filter @core/ai-agents build

# Запусти (с примером фичи из main.ts)
pnpm --filter @core/ai-agents start
```

**Запуск queue worker (24/7):**

```bash
pnpm --filter @core/ai-agents start:queue
# Ctrl+C — graceful shutdown
```

---

## 📊 Текущее состояние проекта

### Что есть ✅

- **Orchestrator v4 утвержден** — production-ready с retry, circuit breaker, budget control и интеграцией с BullMQ.
- **MCP Framework** — 5 серверов, корректно подключены к агентам.
- **Инфраструктура Очередей** — BullMQ работает с frontend paused/resume state.
- **Базовый UI** — Admin Dashboard и Landing Page визуализаторы были протестированы (но требуют синхронизации).

### Переход к Orchestrator v5 🚀

Сейчас мы начинаем разработку **Orchestrator v5**. Основной фокус: абсолютная изоляция задач, строгий сбор метрик и предсказуемость кода.
Перед началом работы над v5 все сторонние ветки (feature/\*) **будут удалены**, останется только `main` (clean slate).

| Фича v5                        | Описание                                                                                                                                                                                      | Приоритет |
| ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| **Strict Git Branching**       | Каждая задача получает свою изолированную ветку `feature/task-{id}`. Код коммитится только туда.                                                                                              | 🔴 High   |
| **Task Dependency Resolution** | Задачи могут влиять друг на друга. Если Задача B зависит от Задачи A, Оркестратор ждет полного мержа ветки Задачи A в `main` перед стартом Задачи B, чтобы избежать конфликтов кодогенерации. | 🔴 High   |
| **Рендеринг логов в UI**       | Отказ от единого `orchestrator.log`. Логи каждой задачи пишутся в `logs/task-{id}.log` и сохраняются в PostgreSQL (`executionLog`).                                                           | 🟡 Medium |
| **ROI Metrics & Costs**        | Сохранение точных затрат токенов, прайсинга моделей и "Сэкономленных человеко-часов" (roiMetrics JSON).                                                                                       | 🟡 Medium |
| **Синхронизация Admin Panel**  | Перевод `QueueList.tsx` на polling (или SSE) для отображения прогресса в реальном времени.                                                                                                    | 🟡 Medium |

---

## 💡 Варианты первых фич

Что логично делать первым — с учётом того, что auth уже частично есть (`feat(auth)` в истории коммитов).

### Фича 1 — Профиль пользователя 👤

**Почему сейчас:** auth есть, JWT работает — добавить страницу куда идти после логина.

```
Страница /profile:
- Показать имя, email из JWT
- Загрузка/смена аватара
- Кнопка выхода (logout)
```

Агенты: `frontend-ui` (UI) + `frontend-bizlogic` (useProfile hook) + `backend-api` (GET /users/me) + `backend-database` (avatarUrl в schema)

---

### Фича 2 — Dashboard с метриками 📊

**Почему сейчас:** первое что видит пользователь после логина должно быть полезным.

```
Страница /dashboard:
- Карточки с ключевыми метриками
- Загрузка данных через TanStack Query
- Скелетоны при loading, error boundary
```

Агенты: `frontend-ui` (cards, layout) + `frontend-bizlogic` (queries) + `backend-api` (endpoints)

---

### Фича 3 — Notifications / Toast система 🔔

**Почему сейчас:** нужна для всех остальных фич — feedback на actions.

```
Global toast provider:
- Success / Error / Warning / Info варианты
- Auto-dismiss с таймером
- Stacking нескольких тостов
```

Агенты: только `frontend-ui` — чистая UI фича, нет backend зависимостей.

---

### Фича 0 — Budget persistence fix 🛠️

**Рекомендация:** если планируешь запускать orchestrator в 24/7 режиме — сначала это.

```
Перенести monthlySpend из памяти в Redis
Ключ: budget:monthly:{year}:{month}
TTL: конец месяца
```

Это не фича продукта, но критичная инфраструктурная починка.

---

## ▶️ Запуск первой фичи через Orchestrator

### Шаг 1 — Определи фичу в `main.ts`

```typescript
const feature: FeatureRequest = {
  id: "FEAT-002",
  title: "User Profile Page",
  description: "Страница профиля с именем, email, аватаром и кнопкой logout",
  acceptanceCriteria: [
    "GET /api/users/me возвращает { id, email, name, avatarUrl }",
    "Страница /profile отображает данные текущего пользователя",
    "Можно загрузить аватар (multipart/form-data)",
    "Кнопка logout очищает токены и редиректит на /login",
  ],
  complexity: "medium",
  priority: "high",
  businessValue: "Базовый user experience после аутентификации",
};
```

### Шаг 2 — Убедись что инфраструктура поднята

```bash
docker compose up -d && docker compose ps
```

### Шаг 3 — Запусти

```bash
pnpm --filter @core/ai-agents build && pnpm --filter @core/ai-agents start
```

### Шаг 4 — Наблюдай за логами

Orchestrator пишет structured JSON через `pino`. Для читаемого вывода:

```bash
pnpm --filter @core/ai-agents start | npx pino-pretty
```

Ты увидишь:

- `[orchestrator]` — Gemini декомпозирует задачу на атомарные шаги
- `[architect]` — Phase 1, quality gate
- `[frontend-ui]`, `[backend-api]` и др. — параллельное выполнение
- Итоговый cost report (input + output tokens, USD)

---

## 🎯 Рекомендованный порядок

```
1. docker compose up -d
2. cp .env.example .env && заполнить ключи
3. pnpm typecheck ← убедиться что нет ошибок
4. /mcp в Claude Code ← проверить 5 серверов
5. Выбрать фичу → запустить через Claude Code (быстро) или Orchestrator (автономно)
```

Всё готово. Осталось выбрать что строим первым. 🏗️
