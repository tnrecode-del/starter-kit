# Session 2026-03-01

> Branch: `main` (merged from `feature/task-feat-6-user-role-permission`)
> Commits this session: `0b53d57` → `add6ce4`

---

## Что было сделано

### 1. Документация проекта (`0b53d57`)

Создано 5 новых файлов в `docs/`:

| Файл                          | Содержание                                               |
| ----------------------------- | -------------------------------------------------------- |
| `docs/project-status.md`      | Git-состояние, tech stack, known issues                  |
| `docs/database-schema.md`     | 5 Prisma-моделей, поля, типы, seed данные                |
| `docs/api-reference.md`       | tRPC (5 процедур) + Next.js route handlers + NestJS REST |
| `docs/ai-agents-reference.md` | 8 агентов, фазы, pricing, MCP, BullMQ                    |
| `docs/frontend-components.md` | Виджеты, фичи, shared UI, Recharts, i18n                 |

Также завершён UI текущей ветки: `TaskDetailsPanel` (Metrics tab с Recharts), `AdminDashboardMetrics` виджет, `chart.tsx` Recharts wrapper.

---

### 2. Анализ готовности v4-агента (`6ec2e9a`)

Запущены 3 параллельных агента для анализа `docs/research/`, `docs/archive/`, оркестратора.

**Вердикт: 6.5/10 — "Работает, но не автономен"**

Критические блокеры:

- **Нет tool-use loop** в `dispatchAgent()` — MCP-инструменты объявлены, но не вызываются
- **Код генерируется как текст**, не записывается на диск
- **Две очереди** (Prisma runLoop + BullMQ) работают независимо — риск двойной обработки

Создан `docs/plans/2026-03-01-v4-autonomous-agent-readiness.md` с P0-P4 roadmap.

---

### 3. Реструктуризация docs/ (`3119531`)

Папки приведены к соответствию смыслу:

```
docs/ai/  + docs/v4/ai/  → docs/research/   (merged, deduplicated)
docs/v4/                 → docs/archive/     (спека, только справка)
                            docs/plans/       (реализационные планы)
                            docs/INDEX.md     (навигационная карта)
```

Добавлены приоритетные тиры: 🟢 быстрый контекст / 🔵 архитектура / 🟠 планы / 🟡 исследования / 🔴 архив.

---

### 4. Метрики в AdminPage (`add6ce4`)

Три параллельных субагента проверили:

1. **backend-database**: найдены критические проблемы — `AdminDashboardMetrics` не рендерился, ключ `role` ≠ `agent` в agentsUsed, нет `include: executionMetric` в запросе
2. **Explore (skills)**: подтверждено — `.agents/skills/` READY (35 директорий, 195 SKILL.md файлов), context-manager корректно подключён
3. **frontend-ui**: реализованы исправления в `AdminPage.tsx`

**Изменения в `apps/web/src/views/admin/ui/AdminPage.tsx`:**

```
+ include: { executionMetric: true, roiMetric: true }  в Prisma-запросе
+ computeTopAgents()    — flatMap agentsUsed, маппинг role→agent, суммирование по агенту
+ computeTotalSpend()   — сумма totalCostUsd по всем задачам
+ computeTasksPerDay()  — группировка createdAt по датам (7 дней)
+ <AdminDashboardMetrics /> рендерится над <QueueList /> с 7 пропсами
```

---

## Ключевые открытия

### Несоответствие ключей в agentsUsed

Seed-данные сохраняют `{ role: "Developer Agent", cost: 0.8 }`, но `AdminDashboardMetrics` ожидает `{ agent, cost }`. Исправлено маппингом в `computeTopAgents()`:

```ts
costByAgent.set(entry.role, (costByAgent.get(entry.role) ?? 0) + entry.cost);
// затем: [agent, cost] = [role, summedCost]
```

### budgetLimit не хранится в БД

Значение существует только как env var `BUDGET_LIMIT` в конфиге оркестратора. В Prisma-схеме нет поля. Захардкожено как `const budgetLimit = 50` в AdminPage.

### Skills-система готова

`.agents/skills/` содержит 195 SKILL.md файлов. Context-manager читает их через filesystem MCP и генерирует JSON-брифинг для Phase 2 агентов. Система функциональна.

---

## Следующие шаги (по приоритету)

### P0 — 2-4 часа (немедленная экономия денег)

**Переключить модели в `packages/ai-agents/src/sub_agents_config.ts`:**

| Агент                       | Сейчас   | Нужно          | Экономия |
| --------------------------- | -------- | -------------- | -------- |
| `frontend-ui` (default)     | `sonnet` | `gemini-pro`   | ~4×      |
| `context-manager` (default) | `sonnet` | `gemini-flash` | ~10×     |

```ts
// sub_agents_config.ts — изменить defaultModel:
"frontend-ui":      { defaultModel: "gemini-pro", ... }
"context-manager":  { defaultModel: "gemini-flash", ... }
```

---

### P1 — 1-2 дня (настоящая автономия)

**1. Tool-use execution loop**

Файл: `packages/ai-agents/src/orchestrator_v4.ts` → функция `dispatchAgent()`

```ts
// Добавить после первого вызова anthropic.messages.create():
while (response.stop_reason === "tool_use") {
  const toolUses = response.content.filter((b) => b.type === "tool_use");
  const toolResults = await Promise.all(
    toolUses.map((tu) => mcpClient.callTool(tu.name, tu.input)),
  );
  response = await anthropic.messages.create({
    ...params,
    messages: [
      ...messages,
      { role: "assistant", content: response.content },
      { role: "user", content: toolResults },
    ],
  });
}
```

**2. Code applicator**

Новый файл: `packages/ai-agents/src/code_applicator.ts`

Парсит `resultData` (markdown с блоками кода) по агентам и записывает файлы через filesystem MCP:

```
architect output  → пропустить (только review)
frontend-ui       → apps/web/src/...
frontend-bizlogic → apps/web/src/...
backend-api       → apps/api/src/...
backend-database  → packages/database/prisma/...
qa-testing        → apps/web/tests/... или packages/ai-agents/tests/
```

---

### P2 — 2-3 дня (авторизация + наблюдаемость)

**1. feat-6: RBAC (текущая ветка называлась `feature/task-feat-6-user-role-permission`)**

Проблемы:

- `auth.login` в `packages/trpc/src/router/auth.router.ts` присваивает всем новым пользователям `role: "ADMIN"`
- Нет проверки авторизации в tRPC-процедурах
- REST API `/api/tasks/*` без auth middleware

Что делать:

- Убрать `role: "ADMIN"` из create-upsert (использовать дефолт схемы `USER`)
- Добавить `protectedProcedure` в tRPC context с проверкой JWT
- NestJS Guard для `/admin/*` роутов

**2. Объединить две очереди**

- Убрать Prisma `runLoop()` из `packages/ai-agents/src/main.ts`
- BullMQ Worker в `queue-main.ts` — единственная точка обработки
- При добавлении задачи в `FeatureQueue` через UI → только через `FeatureQueue.addJob()` (BullMQ)

**3. budgetLimit в конфиге**

- Читать из env var `BUDGET_LIMIT` вместо хардкода `50` в AdminPage
- Добавить endpoint `GET /api/config` или передавать через server component props

---

### P3 — 3-5 дней (когнитивная автономия)

- **Task DAG**: определять какие агенты нужны для конкретной фичи (чисто UI → пропустить backend-database)
- **Failure Memory** в ChromaDB: отдельная коллекция `failure-patterns`, semantic search при retry
- **Tier-based QA**: MVP (unit only) / Beta (unit+e2e) / Production (full suite)

---

### P4 — 1+ неделя (масштабирование)

- Extended `ExecutionLog` (46 полей по спеке `docs/archive/db-schema/`)
- Self-repair loop: execute → test → fail → analyze → patch → re-test
- Concurrency scale-up: `concurrency: 1 → 3-5`

---

## Файлы затронутые в сессии

| Файл                                                              | Статус                      | Коммит  |
| ----------------------------------------------------------------- | --------------------------- | ------- |
| `docs/project-status.md`                                          | Создан                      | 0b53d57 |
| `docs/database-schema.md`                                         | Создан                      | 0b53d57 |
| `docs/api-reference.md`                                           | Создан                      | 0b53d57 |
| `docs/ai-agents-reference.md`                                     | Создан                      | 0b53d57 |
| `docs/frontend-components.md`                                     | Создан                      | 0b53d57 |
| `docs/INDEX.md`                                                   | Создан                      | 3119531 |
| `docs/plans/2026-03-01-v4-autonomous-agent-readiness.md`          | Создан                      | 6ec2e9a |
| `docs/research/`                                                  | Создан (merge ai/ + v4/ai/) | 3119531 |
| `docs/archive/`                                                   | Создан (из docs/v4/)        | 3119531 |
| `apps/web/src/views/admin/ui/AdminPage.tsx`                       | Изменён                     | add6ce4 |
| `apps/web/src/features/task-details/ui/TaskDetailsPanel.tsx`      | Изменён (Metrics tab)       | 0b53d57 |
| `apps/web/src/widgets/admin-metrics/ui/AdminDashboardMetrics.tsx` | Создан                      | 0b53d57 |
| `packages/shared/src/ui/chart.tsx`                                | Создан (Recharts wrapper)   | 0b53d57 |
| `packages/database/prisma/seed-feature.ts`                        | Изменён (Extended seed)     | 0b53d57 |

---

### 5. Исправление ошибок отображения Метрик (`fix/metrics-ui`)

- **Краш UI на вкладке Metrics**: Исправлена ошибка `useChart must be used within a <ChartContainer />` путём оборачивания `PieChart` в `ChartContainer` внутри `TaskDetailsPanel.tsx`.
- **Проблема с цветами в Dark Theme**: Графики отображались чёрным цветом.
  - Причина: Использование обёртки `hsl(var(--chart-X))` для oklch-переменных, что ломало CSS парсер.
  - Решение: Добавлены oklch-переменные в класс `.dark` внутри `globals.css` и убраны обёртки `hsl()`, заменены на `var(--color-chart-X)` в `TaskDetailsPanel.tsx` и `AdminDashboardMetrics.tsx`.

### 6. Улучшения UI карточки задач и логирования (Текущая сессия)

- **Метрики по умолчанию**: Вкладка "Metrics" теперь открывается первой. Если данных еще нет, показывается приятная визуализация-заглушка. Решена проблема с внутренним скроллом на десктопе (`h-auto` вместо жесткого `h-[500px]`) — вкладка теперь принимает полную высоту контента.
- **Интуитивный Overview**: Сырой Markdown-вывод генерации кода скрыт под аккордеоном "View Raw Output" (с фиксированной максимальной высотой окна просмотра файлов `max-h-[400px]`).
- **Git Diffs в логах**: Оркестратор (`orchestrator_v4.ts`) теперь фиксирует измененные файлы (через \`git diff --name-status\`) и прикрепляет их в начало \`resultData\`.
- **Отображение измененных файлов**: Вкладка \`Overview & Results\` парсит измененные файлы из вывода оркестратора и выводит их в виде удобного списка с бейджами (Добавлено, Удалено, Изменено).
- **Отдельные файлы логов**: Оркестратор во время работы пишет подробные Markdown-логи для каждой задачи в \`docs/logs/[FEAT-#].md\`.
- **Фейковые логи для истории**: API \`/api/logs/route.ts\` возвращает заглушку этапов исторических логов, если логов от старых задач не найдено.

---

## Для следующей сессии

Открыть в первую очередь:

1. `docs/session/2026-03-01.md` — этот файл
2. `docs/plans/2026-03-01-v4-autonomous-agent-readiness.md` — P0-P4 roadmap
3. `packages/ai-agents/src/sub_agents_config.ts` — для P0 model routing fix
4. `packages/ai-agents/src/orchestrator_v4.ts` — для P1 tool-use loop
5. `packages/trpc/src/router/auth.router.ts` — для P2 RBAC fix
